1. 一段时间后当登录状态过期时，自动尝试重新登录（仍然适用当密码错误时弹窗要求重新输入新的用户名和密码）
    - 执行自动更新或立即同步时，若出现登录状态过期，也需要自动尝试重新登录
2. 点击手动更新状态时，若显示“跳过：无凭证”或其他非成功状态，则需要弹出弹窗报错
3. 经过一段时间后“下次更新时间”会显示一个极大的年份数字，需要修复
4. 去除上次同步时间、下次同步时间、上次同步结果栏右侧的箭头
5. 考虑是否需要针对不同的登录用户分别用不同的文件存储其成绩信息
